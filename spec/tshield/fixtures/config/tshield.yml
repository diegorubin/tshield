---
grpc:
  port: 5678
request:
  timeout: 0
domains:
  'example.org':
    name: 'example.org'
    filters:
      - 'ExampleFilter'
    paths:
      - '/api/one'
      - '/api/two'
    skip_query_params:
      - 'a'
  'example.com':
    name: 'example.com'
    paths:
      - '/api/three'
