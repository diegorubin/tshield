---
request:
  timeout: 0
domains:
  'example.org':
    name: 'example.org'
    filters:
      - 'ExampleFilter'
    paths:
      - '/api/one'
      - '/api/two'
  'example.com':
    name: 'example.com'
    paths:
      - '/api/three'
